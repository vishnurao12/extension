configuration:
  bdood:
    type: String
    validate:
      isRegex: /^[a-zA-Z_][a-zA-Z0-9_]*$/
  test:
    type: String
  slack:
    type: Integration
    validate:
      isIntegrationType: Slack
  foo:
    type: Int
  bar:
    type: Boolean
  koo:
    type: String
    immutable: true
